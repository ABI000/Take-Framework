using System.CommandLine;

namespace TakeFramework.CLI;

public static class CommandExtensions
{
    public static RootCommand AddCommand(this RootCommand rootCommand)
    {

        return rootCommand;
    }
}
